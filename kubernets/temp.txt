Ingress

Cơ bản về Helm: quản lý gói (khá giống khái niệm của apt, nhưng dùng cho k8s chứ ko phải ubuntu)
Trong này, các gói được gọi là chart

   81  wget https://get.helm.sh/helm-v3.18.3-linux-amd64.tar.gz
   82  l
   83  ls
   84  clear
   85  tar xvf helm-v3.18.3-linux-amd64.tar.gz
   86  ls
   87  clear
   88  ls
   89  sudo mv linux-amd64/helm /usr/bin/
   90  helm versio
   91  helm version

     95  helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx/
   96  helm repo update
   97  clear
   98  helm search repo nginx

Mở file value:
1. ĐỔi type từ Load Balancer (chỉ dùng cho môi trường cloud) ==> NodePort
2. chinh IP http là 30080 và 30443 cho https

Lưu ý cho tới chỗ này, khi gõ kubectl get no, nó phải trả về giá tị như thê snafy
[k8s-master-1] ~ kubectl get nodes

NAME           STATUS   ROLES                  AGE     VERSION
k8s-master-1   Ready    control-plane,worker   2d10h   v1.30.14
k8s-master-2   Ready    control-plane,worker   2d10h   v1.30.14
k8s-master-3   Ready    control-plane,worker   2d10h   v1.30.14

➜  [k8s-master-1] ~ helm -n ingress-nginx upgrade --install ingress-nginx -f ingress-nginx/values.yaml ingress-nginx
Release "ingress-nginx" has been upgraded. Happy Helming!
NAME: ingress-nginx
LAST DEPLOYED: Mon Jul  7 18:44:19 2025
NAMESPACE: ingress-nginx
STATUS: deployed
REVISION: 2
TEST SUITE: None
NOTES:
The ingress-nginx controller has been installed.
Get the application URL by running these commands:
  export HTTP_NODE_PORT=30080
  export HTTPS_NODE_PORT=30443
  export NODE_IP="$(kubectl get nodes --output jsonpath="{.items[0].status.addresses[1].address}")"

  echo "Visit http://${NODE_IP}:${HTTP_NODE_PORT} to access your application via HTTP."
  echo "Visit https://${NODE_IP}:${HTTPS_NODE_PORT} to access your application via HTTPS."

An example Ingress that makes use of the controller:
  apiVersion: networking.k8s.io/v1
  kind: Ingress
  metadata:
    name: example
    namespace: foo
  spec:
    ingressClassName: nginx
    rules:
      - host: www.example.com
        http:
          paths:
            - pathType: Prefix
              backend:
                service:
                  name: exampleService
                  port:
                    number: 80
              path: /
    # This section is only required if TLS is to be enabled for the Ingress
    tls:
      - hosts:
        - www.example.com
        secretName: example-tls

If TLS is enabled for the Ingress, a Secret containing the certificate and key must also be provided:

  apiVersion: v1
  kind: Secret
  metadata:
    name: example-tls
    namespace: foo
  data:
    tls.crt: <base64 encoded cert>
    tls.key: <base64 encoded key>
  type: kubernetes.io/tls

Cài thêm một máy Balancer trước ingress nữa