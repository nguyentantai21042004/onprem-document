apiVersion: apps/v1
kind: Deployment
metadata:
  name: curriculum-vitae
  namespace: personal
  labels:
    app: curriculum-vitae
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: curriculum-vitae
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: curriculum-vitae
      namespace: personal
    spec:
      imagePullSecrets:
      - name: harbor-secret
      containers:
      - name: curriculum-vitae
        image: harbor.ngtantai.pro/personal/curriculum-vitae:250713-163704
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        env:
        - name: NODE_ENV
          value: "production"
        - name: HARBOR_REGISTRY
          value: "harbor.ngtantai.pro"
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: curriculum-vitae-service
  namespace: personal
  labels:
    app: curriculum-vitae
spec:
  selector:
    app: curriculum-vitae
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: curriculum-vitae-ingress
  namespace: personal
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: curriculum-vitae.ngtantai.pro
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: curriculum-vitae-service
            port:
              number: 80